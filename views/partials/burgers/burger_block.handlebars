<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newDevour="{{devour}}">
		{{#if devour}}Devour!{{else}}Have Again!{{/if}}
	</button>

	<!--<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>-->
</li>